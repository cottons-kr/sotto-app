import { uiStyle } from '@/styles/layer.css';

export const weightStyles = {
	regular: uiStyle({
		fontWeight: '400',
	}),
	medium: uiStyle({
		fontWeight: '500',
	}),
	strong: uiStyle({
		fontWeight: '600',
	}),
};
